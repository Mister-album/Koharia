- [x] `KomgaServerPreferences` 成功创建并能在依赖注入图中获取。
- [x] 初次启动时，系统能够自动生成一个带有原始 `KomgaSource.ID` 的默认服务器配置，保证老用户数据不丢失。
- [x] `AndroidSourceManager` 能够根据配置列表动态注册对应的 `KomgaSource` 实例。
- [x] 不同的 `KomgaSource` 实例具有不同的 `Source.name`，确保后续的下载和缓存路径能够物理隔离。
- [x] “更多”页面中成功进入“服务器管理”列表页。
- [x] 能够成功添加新的服务器，新服务器在底层分配了全新的 `Source ID`。
- [x] 书架页顶部工具栏右侧正确显示了切换服务器的按钮。
- [x] 点击切换按钮可呼出服务器列表，选择其他服务器后，书架内容能够切换至对应服务器的缓存/在线数据，没有发生崩溃或数据错乱。
- [x] 删除服务器的逻辑运作正常，且保证最后剩余的一个服务器不可被删除。
